export const home_content = {
    hero:{
        h2: "Welcome BeTheLi9ht Foundation",
        h4: "In Memory of Copy St. Phard",
    }
}

export const home_text_context:string[] = []

for (const [keys, values] of Object.entries(home_content)){
    // home_text_context.push(values)
}